# Helper umbrella for the future ESP-IDF component tree.
# ESP-IDF consumes the per-component CMakeLists.txt files located in each
# component directory.

set(SPOLSENSE_COMPONENTS
    app
    board
    diagnostics
    display
    hx711
    pn532
    spool_tag
)
