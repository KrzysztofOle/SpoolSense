{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run firmware (USB)",
            "type": "node-terminal",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "command": "${workspaceFolder}/tools/run_firmware.sh"
        }
    ]
}
